@typeparam Titem

@if(ListaItem == null){
    
    if(Carregando == null){ // Se o carregando não for chamado essa será a resposta padrão 
        <img src="">
        <text>Carregando</text>
    } else{
        @Carregando
    }

} else if(!ListaItem.Any()){  
    if(SemRegistro == null){ // Se o SemRegistro não for chamado essa será a resposta padrão
        <img src="">
        <text>Não existem tarefas</text>
    } else{
        @SemRegistro
    }
} else{
    foreach(var elemento in ListaItem){
        @Lista(elemento); // elemento sendo passado como context
    }
}

@code{
    [Parameter] public RenderFragment Carregando { get; set; }
    [Parameter] public RenderFragment SemRegistro { get; set; }
    [Parameter] public List<Titem> ListaItem { get; set; }
    [Parameter] public RenderFragment<Titem> Lista { get; set;}
}